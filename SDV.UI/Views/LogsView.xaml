<UserControl x:Class="SDV.UI.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:SDV.UI.ViewModels"
             Background="#F9FAFB">

    <UserControl.DataContext>
        <vm:LogsViewModel/>
    </UserControl.DataContext>

    <Grid Margin="35,30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Seguridad - Auditoría de Sistema" FontSize="20" FontWeight="SemiBold" Foreground="#111827"/>
            <TextBlock Text="Historial completo de transacciones" Foreground="#6B7280" FontSize="12" Margin="0,4,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Background="White" CornerRadius="8" BorderBrush="#E5E7EB" BorderThickness="1" Padding="15" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Filtrar por Fecha:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,10,0"/>

                <DatePicker SelectedDate="{Binding FechaSeleccionada, UpdateSourceTrigger=PropertyChanged}" 
                            Width="150" Height="30" VerticalContentAlignment="Center"/>

                <Button Command="{Binding LimpiarFiltroCommand}" Content="Mostrar Todo" 
                        Margin="15,0,0,0" Padding="10,0" Background="#EFF6FF" Foreground="#1D4ED8" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <Button Command="{Binding CargarLogsCommand}" Content="🔄 Actualizar" 
                        Margin="10,0,0,0" Padding="10,0" Background="Transparent" Foreground="#6B7280" BorderThickness="1" BorderBrush="#E5E7EB" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="White" CornerRadius="12" BorderBrush="#E5E7EB" BorderThickness="1">
            <DataGrid ItemsSource="{Binding ListaLogs}" 
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      Background="Transparent" BorderThickness="0"
                      RowHeight="45"
                      Margin="5">

                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Padding" Value="10"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Foreground" Value="#4B5563"/>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID LOG" Binding="{Binding IdLog}" Width="70" FontWeight="Bold"/>
                    <DataGridTextColumn Header="FECHA" Binding="{Binding FechaHora, StringFormat='dd/MM/yyyy HH:mm:ss'}" Width="150"/>
                    <DataGridTextColumn Header="USUARIO" Binding="{Binding Usuario}" Width="120" FontWeight="SemiBold"/>

                    <DataGridTemplateColumn Header="ACCIÓN" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Accion}" VerticalAlignment="Center" FontWeight="Bold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Accion}" Value="INSERT">
                                                    <Setter Property="Foreground" Value="#059669"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Accion}" Value="UPDATE">
                                                    <Setter Property="Foreground" Value="#2563EB"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Accion}" Value="DELETE">
                                                    <Setter Property="Foreground" Value="#DC2626"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="TABLA" Binding="{Binding Tabla}" Width="140" FontStyle="Italic"/>
                    <DataGridTextColumn Header="DETALLE / DATOS" Binding="{Binding Detalle}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Grid Grid.Row="3" Margin="0,15,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="{Binding TextoPaginacion}" VerticalAlignment="Center" Margin="0,0,15,0" Foreground="Gray"/>

                <Button Command="{Binding AnteriorPaginaCommand}" Content="❮ Anterior" Width="90" Height="30" Margin="0,0,5,0"/>
                <Button Command="{Binding SiguientePaginaCommand}" Content="Siguiente ❯" Width="90" Height="30"/>
            </StackPanel>
        </Grid>

    </Grid>
</UserControl>